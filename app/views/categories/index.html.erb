<h2>Categories</h2>

<ul>
  <% @categories.each do |category| %>
    <li><%= category.name %> | <%= link_to 'Delete', category_path(category), method: :delete, data: { confirm: 'Are you sure?' } %></li>
  <% end %>
</ul>

<%= link_to 'New Category', new_category_path %>
